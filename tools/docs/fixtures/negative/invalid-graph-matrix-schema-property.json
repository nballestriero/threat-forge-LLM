{
  "id": "invalid-graph-matrix-schema-property",
  "description": "A graph part contains a node field rejected by the canonical Graph Part schema.",
  "validator": "format",
  "expected_error": "does not allow additional property unexpected_schema_field",
  "mutations": [
    {
      "path": "docs/reference/project-model/graph/cross-area.graph.yml",
      "replace": "  description: Gate that validates the governed documentation folder and file layout.\n- id: GATE-DOCS-CHECK",
      "with": "  description: Gate that validates the governed documentation folder and file layout.\n  unexpected_schema_field: invalid\n- id: GATE-DOCS-CHECK"
    }
  ]
}
