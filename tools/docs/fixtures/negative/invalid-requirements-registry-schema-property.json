{
  "id": "invalid-requirements-registry-schema-property",
  "description": "A requirements part contains a requirement field rejected by the canonical Requirements Part schema.",
  "validator": "format",
  "expected_error": "does not allow additional property unexpected_schema_field",
  "mutations": [
    {
      "path": "docs/reference/project-model/requirements/markdown-format.requirements.yml",
      "replace": "  scope: repository\n  preconditions:",
      "with": "  scope: repository\n  unexpected_schema_field: invalid\n  preconditions:"
    }
  ]
}
