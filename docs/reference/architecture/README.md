# Architecture reference

## Purpose

Hold architecture reference material that defines approved implementation patterns before runtime code is introduced.

## Schema

Current architecture reference files:

- `backend-composition-pattern.md`

## Rules

Architecture reference documents must remain technical references and must not replace decisions, requirements, or operational workplans.

Backend implementation must default to approved factory/composition-root dependency composition unless a later accepted decision supersedes that rule.
