# Graph model reference

This directory will contain the future graph model reference.

The graph model should explain how documentation, requirements, decisions, components, tests, and traceability records can be transformed into graph nodes and relationships.

Neo4j import support is a future capability, not part of the initial repository setup.
