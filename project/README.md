# Project operations

This directory contains operational project state that is not part of the stable documentation corpus.

Use it for workplans, temporary checkpoints, and handoff material.

Do not treat files in this directory as product documentation unless they are explicitly promoted into `docs/`.
